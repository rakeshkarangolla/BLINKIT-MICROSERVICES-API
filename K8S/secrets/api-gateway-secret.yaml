# =============================================================================
# api-gateway-secret — gateway has no DB; only the shared JWT validation key.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secret
  namespace: blinkit
type: Opaque
stringData:
  JWT_SECRET: "blinkit-super-secure-jwt-secret-key-for-aks-deployment-2026-platform"
