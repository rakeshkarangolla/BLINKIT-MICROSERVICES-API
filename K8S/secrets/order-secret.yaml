# =============================================================================
# order-secret — order-service env (DB + JWT validation secret)
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: order-secret
  namespace: blinkit
type: Opaque
stringData:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://blinkit-postgres.postgres.database.azure.com:5432/blinkit_orders?sslmode=require"
  SPRING_DATASOURCE_USERNAME: "pgadmin"
  SPRING_DATASOURCE_PASSWORD: "Cloud@123"
  JWT_SECRET: "blinkit-super-secure-jwt-secret-key-for-aks-deployment-2026-platform"
