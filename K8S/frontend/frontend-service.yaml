apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: blinkit
  labels:
    app: frontend

spec:
  type: LoadBalancer

  selector:
    app: frontend
    version: green

  ports:
  - name: http
    port: 80
    targetPort: 80